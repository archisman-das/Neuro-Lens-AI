# Spaces-deploy requirements - minimal runtime stack only.
#
# What we DO ship:
#   - numpy / pillow / opencv-headless: image processing for feature extraction
#   - scikit-image: GLCM + entropy texture features
#   - onnxruntime: classifier + segmentation inference (no torch needed in prod)
#   - requests: HF Inference Providers + Ollama HTTP clients
#
# What we DO NOT ship (kept out to fit the free Spaces image quickly):
#   - torch, torchvision: training only, not needed for ONNX inference
#   - tensorflow: legacy .h5 path is unreachable
#   - segmentation_models_pytorch: only needed for the .pt loader
#   - matplotlib, jupyter, monai, albumentations, etc.: dev-only

numpy>=1.26,<3.0
pillow>=10.0
opencv-python-headless>=4.9
scikit-image>=0.22
scikit-learn>=1.3
requests>=2.31
onnx>=1.16
onnxruntime>=1.18
pyyaml>=6.0
# huggingface_hub: pulls ONNX weights from the separate Tubai01/neurolens-models
# Model repo on container boot (we don't bundle them inside the Space repo
# because the Space free-tier cap is 1 GB total).
huggingface_hub>=0.24
